//! `/plan` command handler.

use super::super::types::CommandResult;

/// Handler for `/plan` — stub until plan mode is implemented.
pub fn cmd_plan() -> CommandResult {
	CommandResult::Message("Plan mode not yet implemented.".to_owned())
}
